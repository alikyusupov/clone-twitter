// const router = require("express").Router();
// const express = require("express");
// const { isAuth } = require("../middlewares/isAuth");
// const groupController = require("../controllers/groupController")

// // new Group
// router.post("/", express.json(), isAuth, groupController.createGroup)

// // get user Group
// router.post("/:userId", express.json(), isAuth, groupController.getGroup)

// // add user to Group
// router.post("/add/:userId", express.json(), isAuth, groupController.addMember)

// module.exports = router;
